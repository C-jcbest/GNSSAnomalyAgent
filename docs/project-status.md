# 项目状态

更新：2026-09-23。当前只开展**北京时间每日15点、180日日网格**上的异常定位方法比较。任务是报告N/E/U分量和含端点的索引/日期，不从曲线判定故障、真实形变或滑坡。原144例比较及仅保留原生缺测的36例事后敏感性实验均已完成。方案见[实验方案](experiment-plan.md)，两轮口径见[日同期比较协议](daily-comparison-protocol.md)与[原生缺测协议](daily-native-only-protocol.md)。

## 数据和冻结

- 已保存18个月本地平台快照；`daily15-v2`保留17站、4,731日网格与六个180日候选窗（开发2、验证1、测试3），开发两窗同组。平台四组的固定开发2／验证1／测试1划分未调整；新案例全部继承来源组。
- 新集`data/processed/daily-comparison-v1/`由只读日包构建：六窗×六种受控注入情景（含未注入）×四种缺测状态=144案例，开发48、验证24、测试72。输入不含注入类型/真值；`labels.json`仅用于评测。原生变化没有现场密集标签，未注入不代表正常。
- 配置`configs/daily-comparison-v1.json`预定三个数值检测器、纯视觉及`daily_union`。后者复用**同轮同例**的视觉响应与Hampel独立结果，取并集并保留两路轨迹；与旧数值候选交给视觉重判的`fixed`不同。视觉默认`qwen3.8-flash`且关闭思考；记录实际响应型号、用量、失败和请求。组合行的模型请求是增量0，评估完整成本须计入其两路组成分支。
- 验证运行全部合法后，`artifacts/daily-comparison-v1/frozen-config.json`在不依据弱标签调阈值的前提下冻结了数据集及完整配置哈希；测试门控拒绝更改配置和限量运行。没有访问平台或修改原始、历史运行/标签。

## 本轮结果

已落盘`runs/daily-comparison-dev-v1/`的240/240条、`runs/daily-comparison-validation-v1/`的120/120条和`runs/daily-comparison-test-v1/`的360/360条结果，均无技术失败。以下F1只量化**观测日×分量的注入掩码一致度**，不是现场准确率；来源与派生不独立，不作显著性推断。

| 划分 | Hampel | CUSUM | Isolation Forest | 纯视觉 | 同期视觉∪Hampel |
| --- | ---: | ---: | ---: | ---: | ---: |
| 开发 | 0.01979 | 0.06867 | 0.06869 | 0.32413 | 0.32491 |
| 验证 | 0.08344 | 0.20319 | 0.07213 | 0.11354 | 0.12996 |
| 测试 | 0.04229 | 0.11998 | 0.04763 | 0.08970 | 0.10610 |

测试上CUSUM的F1为0.11998，高于同期并集0.10610及纯视觉0.08970；并集比纯视觉增0.01640，但没有超过最好的单方法，不能声称组合全面胜出。该比较只针对单个测试来源组及固定注入，不能据此认定跨场地泛化。精确计数、P/R和分组见各运行`summary.json`和工作台；不按这些结果回调阈值。测试所有完成的未注入窗均有报告，这只是未知原生背景上的标签外报告，不是已确认现场误报。正常真值组F1为N/A。

低分的目标时长、背景尺度及标签外报告拆解见[日比较诊断](daily-comparison-diagnosis.md)；它是测试后的解释，不作为本次方法或阈值再选择依据。

## 原生缺测事后实验

按用户要求构建`data/processed/daily-comparison-native-v1/`：仅保留原144例中的36个`native`案例，开发12、验证6、测试18；**没有注入新的缺测**，原始缺口保留。36个输入文件与旧集对应文件SHA256一致，逐案例标签对象相同，来源划分不变。开发60/60、验证30/30、测试90/90条方法记录均完整且无技术失败；验证后冻结相同五方法配置，未用标签调阈值。新运行依次在`runs/daily-native-{dev,validation,test}-v1/`，配置凭据在`artifacts/daily-native-v1/`。

| 划分 | Hampel | CUSUM | Isolation Forest | 纯视觉 | 同期视觉∪Hampel |
| --- | ---: | ---: | ---: | ---: | ---: |
| 开发 | 0.01985 | 0.06601 | 0.07649 | 0.38406 | 0.38598 |
| 验证 | 0.08746 | 0.19281 | 0.06849 | 0.10876 | 0.12584 |
| 测试 | 0.04268 | 0.13482 | 0.04884 | 0.10014 | 0.11500 |

测试18例中，CUSUM为TP/标签外报告/FN=205/1712/919，视觉为105/868/1019，并集为128/974/996；较旧144例测试总体略高，但仍不能证明额外缺测是低分主因。旧运行**同18个`native`案例**的三个数值方法预测掩码与新运行完全一致；视觉和并集各18例均出现掩码差异，新视觉F1 0.10014与旧同例0.10010接近。这是重新调用模型后的波动，不能归因于缺测。旧测试结果已被查看，本轮是事后敏感性分析，绝非新的独立盲测；18例仍来自同组三个来源窗，注入掩码F1仍非现场准确率。

## 限制与历史

- 四个来源组中只有三个组形成合格长窗，测试三个窗来自**同一个来源组**；72个派生输入不能视为72个独立场地。缺可信现场正常范围，不能估计现场Precision/F1或证明滑坡预警性能。
- SCWM-04有用户确认的“六月开始明显变化”月级线索，已用于开发辅助，不转逐日密集真值、也不是独立测试。AI背景审核不是现场盲审。
- 历史`daily-pilot-v1/v2`、`fusion-pilot-v1`、`reference_trend`与离线重算只供追溯，不进入本次同期排名。旧离线比较入口和活动文档已移除；清理前代码/文档及SHA256清单位于本地`research/local/cleanup-20260923-daily-restart/`，旧运行产物仍保留。小时采集与收束代码是固定15点日数据的上游，不代表开展小时检测任务。
- 三轮运行的27个Python源码文件及SHA256另封存在本地`research/local/daily-comparison-source-20260923/`；封存源码整体哈希与三轮`run.json`的`source_sha256`一致。运行后工作树又做了格式化、论文图补列并集及防止测试只挑部分方法的门控加固；这些不回写既有预测与指标。复现当时运行应以封存源码及运行身份为准，不能拿当前工作树哈希冒充当时源码。
- 后续只在独立证据支持时考虑轻量Agent；当前不预设其优于单方法或固定组合。指标计算以[指标标准](metrics-criteria.md)为准，历史冻结协议保留在[日预实验协议](daily-pilot-protocol.md)。
- 工作台的日注入运行新增只读“注入标注”图谱，可按来源和缺测状态查看六种情景的三轴曲线、注入区间及原生缺口；标签图不包含检测预测，也不将无新增注入解释为现场正常。显示逻辑不回写任何数据、标签或冻结结果。
