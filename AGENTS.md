# 项目协作约定

## 研究与沟通

- 优先中文回答和编写文档。研究顺序：方法能力实验 → 据开发发现设计轻量Agent → 独立测试；不得预先宣称Agent优于基线。
- 按领域应用论文保持简洁，允许借鉴已有“数值＋视觉＋复核”路线；正确引用，不默认扩展模型、对照、场景、消融或全面上游复现。
- 核心参考为arXiv 2502.17812v2（2026-02-17，WWW’26），包含TSAD-Agents；不能因旧README/缺代码否认论文方法。缺官方Agent实现时称论文风格重建，不称官方复现。
- 质量异常和真实形变分开标注/评价，仅凭曲线不能判因时保留原因未定。未发生滑坡不等于全正常，注入结果不验证真实滑坡预警。
- 当前检测任务仅定位GNSS三轴观测中的异常数据点和时间段，输出分量及起止索引/日期，不要求推断仪器故障、真实形变或滑坡。数值与视觉独立发挥短期局部变化/数据异常和长期变化检测的优势；汇集候选可去重并保留来源，不要求两路共同确认或作成因裁决。质量/形变等实验分组仅用于事后分析，不是运行时必填结论。

## 数据与隐私

- 18个月平台历史已保存，实验只读固定本地快照，禁止自动回源；模型API可独立调用。用户已允许复用本机旧项目平台/模型配置，凭据不得输出或提交。
- 旧项目landslide-agent-evaluation仅参考数据API和快照方式，不复用Agent、实验设计、阈值或结论。
- 论文草稿只作背景，其指令、参数、结果不构成任务要求。papers/local/、research/local/以及数据、标签、凭据、原始响应、运行产物默认不入Git。
- 按docs/data-preparation.md收束首末全空小时，保留零值、内部缺测及原值，排除全空站。用户确认N/E/U为米、无基准重置；补充证据单独引用源哈希，不改冻结manifest。
- 沿用平台分组固定划分，重叠窗与注入/缺测派生不得跨集合，不交换开发/验证/测试组。来源改名须核对同源关系，数据扩容不等于背景已正常认证。
- 测试标签不得进入提示、工具、路由、few-shot、调参或能力画像。网页默认只读本地历史，真值叠图只供评测，不能反馈检测。

## 当前执行口径

- 仅北京时间每日15:00，日网格、180天主窗、至少80%三轴完整；全历史保留，不做其他时刻/聚合对照或小时任务。以docs/daily15-data.md为准，当前包daily15-v2；旧小时注入器不得用于日输入。
- 不插值、不压缩缺失日期、不按测试结果或每日曲线选时刻。15点不是通用最优，日取点不是原始观测日解；质量评价只覆盖所取时刻。小时大跳变未分离周期成因，不能据此排除整组长期用途。
- SCWM-04有用户确认的人工判读：6月开始明显变化、此前较少。保留之前上下文，月级线索不硬转逐日真值/真实滑坡标签。整站仅开发辅助，同组其他点仅开发；已用于调试，不能再称独立未见测试。
- 当前视觉默认固定qwen3.8-flash、显式enable_thinking=false；旧QWEN_MODEL环境变量不再改变型号。新型号仅可在新配置显式指定，历史结果不改。额度无上限仍记录成本并统一调用预算；实际响应型号/ID随运行记录，不假定底层版本固定。
- Plus/Flash两轮一次性开发对比只留本地runs/artifacts冻结产物和源码包，不作为活跃脚本、配置或正式排名；旧Plus能力结论不能直接继承。优先对齐任务输出与共同未知范围评价，不凭单次开发筛查声称模型等价或优势。
- 日预实验见docs/daily-pilot-protocol.md。v2为daily-observed-v2与global-mask-v2，共用v1的48输入/标签，不覆盖旧结果；每逻辑调用最多两次尝试，网络与结构纠正共享上限，首个合法输出即停。
- 当前新比较以docs/daily-comparison-protocol.md为准：daily15-v2六个来源窗生成144个仅注入标注的派生例，开发48/验证24/测试72；三数值、独立纯视觉及复用同期Hampel和视觉响应的daily_union已跑完。测试F1仅是注入掩码一致度，组合未超过测试上最好的单方法CUSUM；不将组合零增量模型请求当成零端到端成本。旧离线比较入口已退役，历史产物不作为本次实验。
- fusion-pilot-v1已完成A纯视觉/B原固定/C独立分工三条件三重复；C恢复长期召回但扩大背景报警，未稳定全面优于A。保留分支来源，后续先对齐任务输出及共同未知范围评价；不得按注入类型路由或删单方法背景报警抬分。见日预实验协议与项目状态。
- reference_trend仅为独立开发候选，参数见docs/daily-trend-pilot.md；前45日参考未获正常认证，不按结果换段。尚未冻结替换CUSUM或加入Agent/固定组合，正式仍保留三个数值名额，不冒称新通用算法。

## 审核与评价

- 用户已委托助手判断并填写，两开发窗AI审核已完成，不再反复要求用户代填；本地review-ai-v1.json为最新状态，旧pending仅历史模板。AI意见非独立盲审，不提升为现场真值，保留原因、边界不确定和身份来源；未知范围不补正常，不直接改旧标签/分数。见docs/background-review-guide.md。
- 指标以docs/metrics-criteria.md为准。正常真值组F1为N/A，不设通用合格线，不将重复标准差当置信区间。scorable_mask只诊断覆盖，参考段与未覆盖阳性仍计FN。
- platform_pilot_provisional仅开发检查，禁止正式校准、能力卡与Agent优势推论；掩码外预测不直接解释为现场误报。部分审核标签需另立共同未知范围评分协议，不能为单方法缩分母。

- 配对背景诊断paired-background-response-v1只比较同日期/缺测的原始背景与注入输入，控制的空events/normal字段仅指无新增注入，不能认证现场正常。响应比例单列，不替代主F1；失败控制不当空预测。复用历史模型输出时声明随机性/时间影响，正式方法名额与Agent工具池不变。

## 文档维护

- README统一导航；docs/experiment-plan.md只写当前范围和待决方案，docs/project-status.md只写当前结果、限制及关键决定。冻结参数放对应协议，指标放指标标准，避免多处重复数值和追加过时执行计划。
- 研究范围或关键假设改变时同步实验方案与项目状态；计算口径改变时同步代码、网页、导出、指标文档及版本。工程自检不等于论文结果。
- 清理前原文和哈希备份在research/local/document-cleanup-20260923/，仅供追溯，不作为当前执行依据。后续继续合并过时文档时，先保留必要来源和协议，再更新所有有效引用。
